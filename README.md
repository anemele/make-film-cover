# Make film cover

Make a folder with structure as below:

```text
Film
  |-image
      |-cover.*
```

This program will generate a `.ico` file with the same name in the `image` folder,
and make `film` folder displayed with a image cover.
